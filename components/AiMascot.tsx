import React from 'react';
import { Topic } from '../types';

interface AiMascotProps {
  topic: Topic;
}

/**
 * Linh thú đã được tắt hoàn toàn từ phiên bản 3.5.0.4.
 * Component được giữ như một lớp tương thích an toàn và không render giao diện.
 */
const AiMascot: React.FC<AiMascotProps> = () => null;

export default AiMascot;
